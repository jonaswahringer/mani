# Keep core help offline

Mani must browse Custom Guides and Official Documentation without an LLM connection, provider account, or network access. LLM-assisted generation is optional and appears only when the user configures a provider, so the core `man` replacement remains dependable in local and restricted environments.
